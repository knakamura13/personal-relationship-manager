# npm configuration
# Note: The "npm warn config production" warning in Railway logs is from Railway's
# build process using the deprecated --production flag. This is harmless and will
# be resolved when Railway updates their build system to use --omit=dev instead.
audit=false
fund=false

